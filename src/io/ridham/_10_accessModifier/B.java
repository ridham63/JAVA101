package io.ridham._10_accessModifier;

// class level modifiers
// possible modifiers: public, <default>, final, abstract, strictfp
// for inner class: public, <default>, final, abstract, strictfp, static, private, protected
public class B {
	// access modifiers are: public, <default>, protected, private

	// public classes can be access from anywhere
	// if a class is public, file-name must match class name

	// <default> classes can be only accessible inside package
	// also known as 'package level access'


}
