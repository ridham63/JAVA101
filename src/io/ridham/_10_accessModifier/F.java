package io.ridham._10_accessModifier;

// member level modifiers
// methods, variables
public class F {

	// public members can be access from anywhere

	// protected members can only be accessible inside pacage or child class
	// when using protected member outside package, object reference should use child class reference

	// <default> members can be only accessible inside package
	// also known as 'package level access'

	// private members can be accessed inside class only
	// private -abstract is illegal combo
}
