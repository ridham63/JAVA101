package io.ridham._11_interfaces;

public class D {
/* Marker Interface : 
 * If an interface dosen't contain any method and by using that interface our object 
 * get some ability its called marker interface.(JVM do its magic internally)
 * 		Serializable, Cloneable, RandomAccess, SingleThreadModel 
 * 
 * -We can't produce our marker interface without custom JVM
 * 
 * 
 * Adapter class:
 * A class that implements an interface and give empty(but concrete) implementation to
 * interface is called adapter class.
 * 		GenericServlet, HttpServlet 
 *   
 */
}
